name: start with gibhubactions
on:
  push:
    branches: 
      -  main
jobs:
  sample:
    runs-on: ubuntu-latest

    steps:
      -  name: checkout code
         uses: actions/checkout@v2

      -  name: install dependencies
         run: npm install

      -  name: run tests
         run: npm tests
